# 🌐 Публикация сайта в интернет

## Да, ваш сайт будет доступен из интернета!

После правильной настройки сервера ваш сайт будет работать как обычный веб-сайт, доступный с любого компьютера в мире по адресу `http://ваш-домен.com` или `http://IP-адрес-сервера`.

---

## ✅ Что нужно для публикации в интернет:

### 1. Облачный сервер с публичным IP
- VPS (Virtual Private Server) от любого провайдера
- Облачный сервер (AWS, DigitalOcean, Hetzner, Timeweb, и т.д.)
- Должен иметь публичный IP-адрес

### 2. Домен (опционально, но рекомендуется)
- Можно использовать IP-адрес напрямую: `http://123.45.67.89`
- Или купить домен и привязать к IP: `http://virtus-prom.com`

### 3. Правильная настройка

---

## 🚀 Пошаговая инструкция

### Шаг 1: Настройте домен (если есть)

Если у вас есть домен (например, `virtus-prom.com`):

1. **В панели управления доменом** добавьте A-запись:
   ```
   Тип: A
   Имя: @ (или оставьте пустым)
   Значение: IP-адрес вашего сервера
   TTL: 3600
   ```

2. **Для поддомена www** добавьте еще одну A-запись:
   ```
   Тип: A
   Имя: www
   Значение: IP-адрес вашего сервера
   TTL: 3600
   ```

**Или используйте IP-адрес напрямую** (без домена):
- Просто откройте `http://ваш-IP-адрес` в браузере

---

### Шаг 2: Настройте .env файл

**Важно!** Укажите правильный URL в `.env`:

```env
# Если есть домен:
APP_URL=https://virtus-prom.com

# Или если используете IP:
APP_URL=http://123.45.67.89

# Обязательно для продакшн:
APP_ENV=production
APP_DEBUG=false
```

---

### Шаг 3: Настройте веб-сервер (Nginx)

Создайте файл `/etc/nginx/sites-available/virtus-prom`:

```nginx
server {
    listen 80;
    server_name virtus-prom.com www.virtus-prom.com;  # или ваш IP
    root /var/www/Virtus-prom/public;
    index index.php;

    # Разрешаем доступ из интернета
    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        fastcgi_pass unix:/var/run/php/php8.2-fpm.sock;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
    }
}
```

**Если используете только IP-адрес**, замените `server_name`:
```nginx
server_name _;  # Принимает любой домен/IP
```

Активируйте:
```bash
sudo ln -s /etc/nginx/sites-available/virtus-prom /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

---

### Шаг 4: Настройте файрвол

Разрешите входящие подключения:

```bash
# Разрешить HTTP (порт 80)
sudo ufw allow 80/tcp

# Разрешить HTTPS (порт 443) - для SSL
sudo ufw allow 443/tcp

# Разрешить SSH (порт 22) - для управления
sudo ufw allow 22/tcp

# Включить файрвол
sudo ufw enable

# Проверить статус
sudo ufw status
```

---

### Шаг 5: Настройте SSL (HTTPS) - РЕКОМЕНДУЕТСЯ

Для безопасности используйте HTTPS:

```bash
# Установите Certbot
sudo apt install certbot python3-certbot-nginx

# Получите SSL сертификат (бесплатно от Let's Encrypt)
sudo certbot --nginx -d virtus-prom.com -d www.virtus-prom.com

# Автоматическое обновление уже настроено
```

После этого сайт будет доступен по `https://virtus-prom.com`

---

### Шаг 6: Обновите APP_URL в .env

```bash
nano .env
```

Измените:
```env
APP_URL=https://virtus-prom.com  # или http://ваш-IP
```

Примените изменения:
```bash
php artisan config:clear
php artisan config:cache
```

---

## ✅ Проверка доступности

### 1. С вашего компьютера:
Откройте браузер и перейдите:
- `http://ваш-IP-адрес` или
- `http://ваш-домен.com`

### 2. С другого компьютера/телефона:
- Откройте тот же адрес
- Должен открыться ваш сайт!

### 3. Проверка из командной строки:
```bash
# С любого компьютера в интернете:
curl http://ваш-IP-адрес
# или
curl http://ваш-домен.com
```

---

## 🔒 Безопасность (ВАЖНО!)

### 1. Отключите отладку
```env
APP_DEBUG=false
APP_ENV=production
```

### 2. Используйте HTTPS
- SSL сертификат защищает данные
- Обязательно для ввода паролей и личных данных

### 3. Сильные пароли
- Для базы данных
- Для пользователей админки

### 4. Регулярные обновления
```bash
sudo apt update && sudo apt upgrade -y
```

### 5. Настройте резервное копирование
```bash
# Ежедневный бэкап базы данных
0 2 * * * mysqldump -u user -p'pass' virtus_prom > /backup/db_$(date +\%Y\%m\%d).sql
```

---

## 🌍 Доступ из интернета

После настройки ваш сайт будет доступен:

✅ **С любого компьютера в мире**  
✅ **С любого мобильного устройства**  
✅ **По домену** (если настроен)  
✅ **По IP-адресу** (если домена нет)  

**Примеры:**
- `https://virtus-prom.com` - если есть домен
- `http://185.123.45.67` - если используете IP
- Работает на Windows, Mac, Linux, Android, iOS

---

## ❗ Частые проблемы

### Сайт не открывается из интернета

1. **Проверьте файрвол:**
   ```bash
   sudo ufw status
   sudo ufw allow 80/tcp
   ```

2. **Проверьте, что Nginx запущен:**
   ```bash
   sudo systemctl status nginx
   ```

3. **Проверьте логи:**
   ```bash
   sudo tail -f /var/log/nginx/error.log
   ```

4. **Проверьте, что сервер слушает на порту 80:**
   ```bash
   sudo netstat -tlnp | grep :80
   ```

### Ошибка "502 Bad Gateway"

- Проверьте, что PHP-FPM запущен:
  ```bash
  sudo systemctl status php8.2-fpm
  ```

### Сайт открывается только с сервера

- Проверьте файрвол
- Проверьте настройки провайдера (может быть блокировка портов)
- Убедитесь, что используете публичный IP, а не локальный (127.0.0.1)

---

## 📝 Краткая шпаргалка

```bash
# 1. Настройте .env
APP_URL=https://ваш-домен.com
APP_ENV=production
APP_DEBUG=false

# 2. Настройте Nginx
sudo nano /etc/nginx/sites-available/virtus-prom
sudo ln -s /etc/nginx/sites-available/virtus-prom /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx

# 3. Откройте порты
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp

# 4. Настройте SSL
sudo certbot --nginx -d ваш-домен.com

# 5. Готово! Откройте сайт в браузере
```

---

## 🎉 Готово!

После выполнения всех шагов ваш сайт будет работать как обычный веб-сайт в интернете!

**Проверьте:**
- Откройте сайт с вашего компьютера
- Откройте с телефона (через мобильный интернет)
- Попросите друга открыть с его компьютера

Все должно работать! 🚀
